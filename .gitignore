# secrets / credentials (never commit)
.env
.env.*
.ambi/
.ambi/config.json
*.key
*-credentials*.json

# node / next
node_modules/
.next/
out/
dist/
web/dist-hosting/
npm-debug.log*
pnpm-debug.log*

# python
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/

# os / editor
.DS_Store
.vscode/
.idea/

# remotion
video/node_modules/
video/out/

# caption pill renders: 143MB of alpha ProRes, editing intermediates for
# iMovie rather than repo content. The PNG stills and the Remotion sources
# under video/src/captions are committed, so any pill can be re-rendered.
demo/captions/*.mov
